## HANDOFF — 2026-07-15 10:00

### Instance Role
Implementer (full-stack, single instance): diagnostics, physics-correction experiment,
implementation, verification, deploys. Sessions 2026-07-10 → 2026-07-15 in one continuous
conversation; the dominant final arc was the **custom-source physics correction**.

### What Was Done
All pushed to `origin/main` (now at `b42894d` + local docs commit `0915a6b`, unpushed):

1. **Three small fixes** (`89bb48b`, 07-10): BHEX "pending sign-off" marker removed
   (framing APPROVED by Marrone/Alejandro; "characteristic ~ R⊕+h" kept verbatim —
   `tourActs.js`, `PhysicsNotesModal.js`, `tourPhysics.js`, `sceneE.js`); compare mode
   zoomed out (`Globe.initialCameraDistance` prop + `COMPARE_CAMERA_DISTANCE=4.2`) +
   per-pane BHEX toggle surfaced (`SimPane.js`); tour scroll-to-top (`Tour.js` bodyRef).
2. **BHEX globe auto-zoom** (`85a36cc`, 07-10): main globe eases camera to 4.5 when BHEX
   toggles on (orbit ring at radius 1.5 was clipped at 2.8) — `Globe.js` animate loop.
3. **Target distances** (`07fb334`, 07-13): sourced distances in `SKY_TARGETS` (cited:
   EHT VI, GRAVITY, Harris+10; 3C 279 = z 0.536 + light-travel ~5.4 Gyr, NOT metric);
   tour Act D shows M87*'s distance via `P.str.m87Distance`/`m87DistLy`.
4. **Dec-readout distance** (`e4ce3a3`, 07-13): `formatTargetDistance()` in constants.js
   (single source, "or" separator); ControlsPanel renders it under Dec:; 3C 279 labeled
   "Light-travel".
5. **Source-image diagnostic + custom-path fixes** (`4aff3a6`, 07-13): uploads/wfu-seal
   now set `selectedTarget='Custom'` (were silently imaged AS M87*); `measureRingFraction`
   gated to shadow targets; Ground Truth panel shows the SCALED source; Invert toggle
   (`controls.invert` → `effectiveGrayscale`). ⚠ Also shipped a zero-spacing suitability
   notice that was WRONG PHYSICS (see 6).
6. **Custom-source physics correction** (`2c3b188`/`01b940c` Phase 0 pushed alone 07-14;
   `b42894d` full pass 07-15): Alejandro rejected the DC framing (correctly). Phase 0
   deleted `SourceNotice.js` + `computeSourceRadialPower`/`assessSourceSuitability`.
   Phase 1: 39-cell measured sweep through the real worker → `CUSTOM-SOURCE-PHYSICS.md`
   + 2 montage JPEGs in `_system/`. Phase 2: two-regime scaling — `CUSTOM_DEFAULT_FOV_UAS=800`
   / `CUSTOM_SOURCE_FRACTION=0.9` seeded on Custom-regime entry (`handleTargetChange` with
   ref-mirrored previous target); "Image size on sky" slider (μas/mas) in ControlsPanel.
   Phase 3: new `ResolutionBudget.js` (Custom only): live N_res/beam/occupancy, the optimum
   lesson, add-elements ladder (2017→2022→ngEHT→+BHEX). Verified: one ladder click makes
   the seal legible.
7. **Housekeeping**: all 8 merged branches deleted; `/sync` run (commit `0915a6b`:
   SESSION-CONTINUITY + _system/MEMORY brought current).

Ring path proved **byte-identical** through every scaling-touching change (CLEAN canvas
FNV hash 2154452775 / Dirty 1389367993 at noise=0, fresh ports, verified repeatedly).
`worker.js` diff EMPTY across the entire span.

### What Was NOT Done
- `.workflows/_prompts/freeze-fix-invert-stress.md` exists untracked — **not read, not
  executed** (appears to be Ilan's next prepared pass).
- `primer.md`-adjacent human TODOs: projector-laptop CLEAN timing gate (SESSION-CONTINUITY
  blocker #3) — still open, human hardware required.
- Sending Alejandro the sweep evidence — Ilan's action, material is ready.
- Compare mode does NOT get the ResolutionBudget panel (single mode only, by design).
- The tour remains M87*-hardwired (distances for other targets are data-only).
- Local docs commit `0915a6b` (knowledge sync) is NOT pushed (docs-only; rides with the
  next push).

### Open Questions
1. Should the ResolutionBudget "sweet spot" become per-array (sweep measured EHT 2017
   ≈800 μas precisely; 2022/ngEHT optima are flatter/larger) — currently copy names only
   the EHT 2017 number as "measured".
2. Custom-regime size slider maxes at 1.0×FOV with FOV clamp 2000 μas; BHEX aliases off
   the uv grid above FOV ≈1,760 μas (mask wraps). Pre-existing, unfixed — decide whether
   to clamp FOV when BHEX is on, or accept and document in-UI.
3. Does Ilan want an `/handoff`-style demo script for the Harvard talk (upload → ladder)?

### Gotchas Discovered This Session
All already written to gotchas.md this session (nothing pending): the zero-spacing
CORRECTION entry (measured-but-tautological statistics; controls isolate causes); htm
drops the space at a newline text↔element boundary (keep the space on the tag's line);
byte-identical hash-gate TECHNIQUE (FNV over canvas ImageData at noise=0, fresh port);
custom-source target-inheritance gotcha (uploads imaged as M87*). Engine limits recorded
in CUSTOM-SOURCE-PHYSICS.md: BHEX off-grid >1,760 μas FOV; fitted beam under-reads λ/B at
small FOV; engine DR clamps at 100.

### New Decisions Made
All already written to decisions.md this session (nothing pending): BHEX framing approved
(hedge removed, characteristic ~ retained, never "="); sourced target distances (3C 279 as
redshift + light-travel, never a metric "distance"); TWO-REGIME SCALING (astrophysical
targets keep target-derived units; Custom images get their own scale, seeded at the
measured 800 μas sweet spot; regime switch = unit switch); teaching copy states only the
corrected physics with live computed numbers, never fires on the ring; old DC verdict
marked SUPERSEDED (banner on SOURCE-IMAGE-DIAGNOSTIC.md).

### Suggested Next Steps
1. **Read `.workflows/_prompts/freeze-fix-invert-stress.md`** — likely the next pass;
   execute per its own protocol.
2. **Push `0915a6b`** (docs-only knowledge sync) with the next deploy, or alone if idle.
3. **Projector timing gate** (human): re-run Act C CLEAN timing on the talk laptop;
   flip presenter-mode Act C to cached playback if >300 ms (fallback already exists).
4. **Ilan → Alejandro**: send CUSTOM-SOURCE-PHYSICS.md + montages.
5. If touching custom-source scaling again: re-verify the ring hash gate (values above)
   and remember the BHEX >1,760 μas aliasing ceiling.

### State of Knowledge Files
- `_knowledge/{codebase,decisions,gotchas}.md` — CURRENT (synced in `0e5903a` + `0915a6b`).
- `_system/SESSION-CONTINUITY.md` — CURRENT as of 2026-07-15 (post-deploy passes block,
  blockers #5/#6/#7 resolved, only #3 open).
- `_system/MEMORY.md` — CURRENT (zero-spacing lesson added; BHEX TODO closed).
- `_system/CUSTOM-SOURCE-PHYSICS.md` + 2 montages — the evidence package for Alejandro.
- `_system/SOURCE-IMAGE-DIAGNOSTIC.md` — carries a SUPERSEDED banner (bug findings valid,
  physics verdict not).
- `primer.md` — rewritten by this handoff.
- Nothing pending for any knowledge file.
