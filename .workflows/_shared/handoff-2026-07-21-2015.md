## HANDOFF — 2026-07-21 20:15

### Instance Role
Implementer + Reviewer (single continuous session). Ran two passes: (1) a read-only
**custom-regime audit** that found a P0 physics defect, then (2) the **P0 restore-beam fix**
that deliberately re-baselined the CLEAN ring hash. The fix is MERGED + PUSHED and LIVE on main.

### What Was Done
**Audit (commit `0712e01`, read-only):** `.workflows/_shared/custom-regime-audit-2026-07-21.md`
— 6 checks. CLEAN verdicts: CHECK 1 (`angularRes` is computed live, not inherited), CHECK 3
(ΔNCC works at 350), CHECK 6 (aliasing caveat silent at default). DEFECTS: CHECK 2 (beam),
CHECK 5 (upload honesty). UNDOCUMENTED: CHECK 4 (why 350 not 300).

**P0 beam fix (merged `8bcbc30`, live; base was `c5331da`):**
- `vlbi-react/js/worker.js` (the ONLY code change — sigma conversion + its comment): the
  restore-beam scan measures the PSF **HWHM** (peak→half-max) but divided by 2.3548 as if FWHM,
  so `beamSigma` was **2× too small since S4** (79b49a9). Fixed, both axes:
  `const fwhmU = 2 * halfWidthU; // HWHM→FWHM` then `beamSigmaU = Math.max(1.5, fwhmU / 2.3548); // FWHM→sigma`.
  One site corrects the restore kernel AND every display consumer (MetricsPanel, ResolutionBudget
  N_res, ContourMap ellipse, FITS BMAJ). Full worker.js diff = exactly this + the comment.
- **Verified:** point-source PSF → displayed Beam FWHM now **20.5 μas = true FWHM** (was 10.25 =
  the HWHM). M87* ~2 beams across the 42 μas shadow (physically correct). N_res 31→15.
- **RING HASH RE-BASELINE: CLEAN `2154452775` → `1397912851`** (M87*/EHT2017/black-hole/noise0/N512),
  reproduced on two never-used ports; verified again post-merge on a 3rd fresh port (poll for
  "Reconstruction complete" + hash-stability — the mount renders transient frames, so a fixed-delay
  hash can catch a mid-transition value; always settle first). **Dirty `1389367993` UNCHANGED**
  (control; restore is post-deconvolution). Ground Truth `1904227387` unchanged.
- Docs (commit `d58ce2b`): decisions.md authoritative old→new entry; gotchas.md HWHM/FWHM trap;
  SESSION-CONTINUITY S4 marked SUPERSEDED; forward-looking prompt-file `<invariants>` hashes
  updated to 1397912851 (dated historical records + the audit doc left at 2154452775 as history).
- Artifact (commit `ab5b5ef`): `.workflows/_shared/beam-fix-before-after-2026-07-21.md` +
  `beam-fix-assets/{before,after}-{m87,sgra}.png`. Ground Truth + Dirty byte-identical before/after;
  CLEAN softer but legibly resolved; systematic across M87* and Sgr A*.
- Regression: Tour Acts A–E render; Act C (dirty→CLEAN) & Act D (First Light) read correctly;
  CLEAN 119 ms (< ~300 ms budget); custom-350 loads; zero console errors.

### What Was NOT Done
- **PASS 2 not started** (it is the next task — see Suggested Next Steps).
- The user-image BHEX ΔNCC window sweep was NOT re-measured — it is INVALIDATED (old beam).
- The Custom 350 default was NOT re-tuned (deferred to Pass 2).
- CHECK 5 (upload content-dependence caveat) NOT implemented (cleared-to-ship, deferred).
- Two prompt files remain untracked (`_prompts/bhex-seal-diagnosis.md`,
  `_prompts/user-image-bhex-window.md`) — their invariant hash was updated in-place but left
  untracked (pre-existing status). Track them if/when desired.

### Open Questions
- CHECK 4: what is the correct Custom default FOV now (300 had the larger delta pre-fix; must be
  re-decided from the RE-SWEPT numbers with the corrected beam) — and document the rationale.
- Does the re-swept BHEX window still land at EHT 2022, or does the wider (correct) beam shift the
  honest window? Re-measure before assuming.

### Gotchas Discovered This Session
Already written to gotchas.md this session (nothing pending):
- HWHM-mislabelled-as-FWHM restore-beam trap (2× too small); ring hashes gate the reconstruction
  ARRAY not derived display values; no display-only fix (the value feeds the CLEAN restore kernel).
- (measurement) the mount renders transient reconstruction frames — hash only after "Reconstruction
  complete" + two-read stability, never at a fixed delay, or you get a false hash.

### New Decisions Made
Already written to decisions.md this session (nothing pending): the CLEAN ring-hash re-baseline
2154452775 → 1397912851 (root cause, Dirty control held, Alejandro sign-off, reproduced two ports).

### Suggested Next Steps
1. **PASS 2 — re-measure the user-image BHEX window with the corrected beam.** The entire ΔNCC
   sweep in CUSTOM-SOURCE-PHYSICS.md ("USER-IMAGE BHEX WINDOW") is invalid (old 2×-narrow beam).
   Re-run the seal + ring-target sweep (scale × array, on-grid) via the real worker; find the honest
   window again; report new numbers.
2. **CHECK 4 — set + DOCUMENT the Custom default FOV** from the re-swept numbers (was undocumented
   350; 300 had the larger pre-fix delta). Write the rationale in the constant comment + doc.
3. **CHECK 5 — add the content-dependence caveat to the upload/custom surface** (cleared to ship,
   no sign-off): e.g. in ResolutionBudget, "Bold, simple shapes resolve most clearly; fine detail
   may stay soft — it needs more ground stations, not just BHEX."
4. Optionally track the two untracked prompt files.

### State of Knowledge Files
- `_knowledge/decisions.md` — CURRENT (re-baseline entry added).
- `_knowledge/gotchas.md` — CURRENT (HWHM/FWHM + transient-hash entries).
- `_system/SESSION-CONTINUITY.md` — CURRENT (LAST UPDATED = this pass, LIVE; S4 SUPERSEDED).
- `_shared/custom-regime-audit-2026-07-21.md` + `beam-fix-before-after-2026-07-21.md` — the audit
  and the before/after artifact (for Alejandro). Nothing pending.
- ⚠ `CUSTOM-SOURCE-PHYSICS.md` "USER-IMAGE BHEX WINDOW" section is now STALE (old-beam numbers) —
  Pass 2 must re-measure and rewrite it. Do not trust its ΔNCC / N_res / beam figures.
