# Bug Fix Run Log — 2026-04-07-run-003

**DATE:** 2026-04-07
**BUG:** Three vlbi-react bugs: coastal land blocked, ocean during load window, MEM removed
**BRANCH:** fix/ocean-telescope-placement
**COMMIT:** 2730125

## Summary
Three distinct issues fixed in one run.

1. isOnLand too restrictive — 110m polygon misses small islands and coastal edges.
   Fixed with 3° vertex proximity buffer after polygon check. Canary Islands, Greek
   islands, and NYC coastal now pass. Deep ocean (Pacific, Atlantic, Indian) still blocked.

2. Ocean clicks during load window — null-state fallback was `return true` (allow all).
   Changed to `return false`. Catch block also sets empty arrays to avoid permanent null.

3. Max Entropy removed from UI — ControlsPanel had only CLEAN checkbox. Restored MEM as
   second checkbox. Controls refactored to useClean/useMem booleans; method computed
   before worker message. restoredLabel covers all three states.

## Diagnosis
All three root causes confirmed before touching code. No guessing.

## Fix
- globeHelpers.js: +25 lines (vertex cache + buffer loop + catch fix)
- App.js: 5 lines changed (method string → two booleans throughout)
- ControlsPanel.js: +9 lines (MEM checkbox)

## Verification
Playwright: all 14 land/ocean assertions pass. UI checkboxes confirmed. Labels correct.
No JS errors. No collateral damage.

## Score: 10/10
All 7 objective pts + all 3 subjective pts. Fix minimal, root causes documented, no
systemic pattern requiring new gotcha (existing "world-atlas 110m limits" gotcha already
covers the 110m dataset limitation; buffer approach is the documented fix path).
