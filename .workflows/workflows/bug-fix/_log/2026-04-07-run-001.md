# Bug Fix Run Log — 2026-04-07-run-001

**DATE:** 2026-04-07
**BUG:** Ocean telescope placement in vlbi-react
**BRANCH:** fix/ocean-telescope-placement
**COMMIT:** 381fa59

## Summary
The same fix already applied to the root app (e396074) was missing from the live
vlbi-react version. Globe.js click handler had no land/ocean guard.

## Diagnosis
Root cause confirmed in 1 pass. No hypotheses ruled out sequentially — reading
Globe.js directly showed the missing guard. Compared against root app's fix approach
(js/landMask.js) to determine the correct implementation pattern.

## Fix
2 files changed: new `vlbi-react/js/landMask.js` (ES module) + 3-line change in
`vlbi-react/js/Globe.js`. Minimal — no refactoring, no scope creep.

## Verification
Playwright browser evaluation confirmed:
- Atlantic Ocean (20°N, 40°W) → isOnLand = false ✓
- Africa (0°N, 20°E) → isOnLand = true ✓
- All 6 EHT presets → land ✓
- No JS errors ✓

## Score: 9/10
-1: No automated regression test (no test runner in project; Playwright manual check
    is the regression baseline for this type of bug)

## Systemic Pattern Added
gotchas.md updated: "Fixes repeatedly applied to root app instead of vlbi-react"
