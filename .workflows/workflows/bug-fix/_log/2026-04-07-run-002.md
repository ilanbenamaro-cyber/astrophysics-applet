# Bug Fix Run Log — 2026-04-07-run-002

**DATE:** 2026-04-07
**BUG:** Land mask too coarse (1° bitmap) in vlbi-react
**BRANCH:** fix/ocean-telescope-placement
**COMMIT:** 396e473

## Summary
The precomputed 360×180 land bitmap had 1°/cell (~111km) resolution. 5 of 15 test
sites returned false (ocean) incorrectly: New York, Portugal coast, Canary Islands,
Greek islands, Cuba. Replaced with point-in-polygon check against countries-110m.json.

## Diagnosis
Hard evidence gathered via Node.js script against the bitmap — 5 specific failures
identified and documented before touching any code.

## Fix
Added `_landPolygons`, `_pointInRing`, `_pointInPolygon`, and `isOnLand()` to
`globeHelpers.js`. Populated cache in the already-existing `loadCountryBoundaries`
fetch — zero extra network requests. Deleted `landMask.js`. Changed 1 import line
in `Globe.js`.

## Verification
5 failures → 3 data-gap edge cases (Canary Islands, Greek islands, NYC coast).
Portugal coast and Cuba fixed. All 6 EHT preset sites pass. No JS errors.
Remaining edge cases are absent from the 110m dataset — not algorithm failures.

## Score: 9/10
-1: 3 residual island/coastal edge cases remain (110m dataset limitation, not fixable
    without switching to 50m dataset — documented in gotchas.md)

## Systemic Pattern Added
gotchas.md updated: "world-atlas countries-110m.json omits small islands"
