# Feature Run Log — WCAG 2.1 AA Accessibility Panel

**Date:** 2026-04-08
**Branch:** fix/ocean-telescope-placement
**Commit:** 59f5b5e

## Features Delivered

1. **Tooltip fix** — `INFO.restored.body` in `constants.js`: "Wiener filter" → "Max Entropy or CLEAN"
2. **WCAG 2.1 AA Accessibility Panel** — new `A11yPanel.js` component with:
   - High contrast toggle (`[data-high-contrast]` CSS overrides, #ffd700 ~14:1 ratio)
   - Font size S/M/L (`--fs-*` CSS custom properties, +2px/+4px overrides)
   - Reduced motion toggle (`[data-reduced-motion]` CSS + Globe.js autoRotate reactive)
   - localStorage persistence + OS `prefers-reduced-motion` initialisation
   - Keyboard (Escape) + click-outside close behaviour

## Files Changed
- `vlbi-react/js/constants.js` — tooltip text fix
- `vlbi-react/css/app.css` — CSS vars, high-contrast, reduced-motion, A11y panel styles (~205 lines added)
- `vlbi-react/js/A11yPanel.js` — new component (~80 lines)
- `vlbi-react/js/App.js` — a11y state, useEffect, A11yPanel render, reducedMotion prop to Globe
- `vlbi-react/js/Globe.js` — accepts reducedMotion prop, reactive autoRotate useEffect

## Verification Results (Playwright)
- App loaded: ✓
- Globe canvas rendered: ✓
- A11y button present: ✓
- Panel opens on click, aria-expanded=true: ✓
- High contrast toggled + persisted on reload: ✓
- Font size set to large + persisted on reload: ✓
- Reduced motion toggled: ✓
- No "Wiener filter" text in page: ✓
- 3 method buttons present (Dirty Only, Max Entropy, CLEAN): ✓
- JS errors (non-network): NONE

## Notes
- Network 404/CORS errors for `earth-clouds.png` (external CDN) are pre-existing and non-blocking
- Custom toggle inputs are visually hidden — must click the `label.a11y-toggle` element, not the `input`
